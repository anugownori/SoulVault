name: soulvault
description: A digital emotional legacy app for Android (Flutter MVP)
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.12.0
  # More dependencies as MVP grows

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true